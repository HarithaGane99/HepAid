# HepAid
Built with Next.js (frontend), Flask (backend), and Python (deep learning).

## Setup
### Frontend (Next.js)
```bash
cd app
npm install
npm run dev

cd backend
pip install -r requirements.txt
python app.py
